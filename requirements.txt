# Serverless (Vercel) runtime dependencies for api/index.py.
# Pinned to the versions the ML artifacts were trained/pickled with.
# Training-only packages (shap, statsmodels, matplotlib, uvicorn, pytest)
# are intentionally excluded to stay under the function size limit.
fastapi==0.139.0
SQLAlchemy==2.0.51
pydantic==2.13.4
pydantic-settings==2.14.2
python-jose[cryptography]==3.5.0
numpy==2.4.6
pandas==3.0.3
scikit-learn==1.9.0
lightgbm==4.6.0
joblib==1.5.3
reportlab==5.0.0
google-genai==2.11.0
